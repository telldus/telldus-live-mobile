#ifndef CONFIG_H
#define CONFIG_H

#define TELLDUS_LIVE_API_ENDPOINT "${TELLDUS_LIVE_API_ENDPOINT}"
#define TELLDUS_LIVE_PUBLIC_KEY "${TELLDUS_LIVE_PUBLIC_KEY}"
#define TELLDUS_LIVE_PRIVATE_KEY "${TELLDUS_LIVE_PRIVATE_KEY}"
#define GOOGLE_ANALYTICS_TRACKER "${GOOGLE_ANALYTICS_TRACKER}"
#define PUSH_SERVICE_ID ${PUSH_SERVICE_ID}

#define RELEASE_BUILD ${IS_RELEASE_BUILD}
#define IS_FEATURE_WEBSOCKETS_ENABLED ${IS_FEATURE_WEBSOCKETS_ENABLED}
#define IS_FEATURE_LOGGING_ENABLED ${IS_FEATURE_LOGGING_ENABLED}
#define IS_FEATURE_GOOGLEANALYTICS_ENABLED ${IS_FEATURE_GOOGLEANALYTICS_ENABLED}
#define IS_FEATURE_PUSH_ENABLED ${IS_FEATURE_PUSH_ENABLED}
#define HAVE_WEBKIT	${HAVE_WEBKIT}

#ifdef PLATFORM_BB10
#  define PUSH_APPID "${PUSH_APPID}"
#  define PUSH_PPGURL "${PUSH_PPGURL}"
#endif

#define VERSION "${PACKAGE_MAJOR_VERSION}.${PACKAGE_MINOR_VERSION}.${PACKAGE_PATCH_VERSION}"

#endif  // CONFIG_H
